node_modules/
.env
*.log
.DS_Store

# Data directory: runtime + user data is never tracked.
# Only curated content (question banks, song/word lists) is allowlisted.
data/*
!data/hitsterSongs.json
!data/pictionaryWords.json
!data/lotrQuestions.json
!data/osrsQuestions.json
!data/pokemonQuestions.json
!data/wowQuestions.json

# Audit report: repo is public — do not publish until the fixes are merged
docs/codebase-audit-2026-08-12.md

# Temporary audio cache files
*.opus
*.webm
godcord_*.opus

# OS generated files
Thumbs.db
ehthumbs.db
.Spotlight-V100
.Trashes

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/
*.sublime-*

# YouTube player scripts (accidentally downloaded)
*-player-script.js
*-base.js

# Local planning docs (public repo - keep working docs out)
docs/superpowers/
