node_modules/
.env
*.log
.DS_Store

# Data directory: runtime + user data is never tracked.
# Only curated content (question banks, song/word lists) is allowlisted.
data/*
!data/hitsterSongs.json
!data/pictionaryWords.json
!data/lotrQuestions.json
!data/osrsQuestions.json
!data/pokemonQuestions.json
!data/wowQuestions.json

# Audit report: repo is public — do not publish until the fixes are merged
docs/codebase-audit-2026-08-12.md

# Temporary audio cache files
*.opus
*.webm
godcord_*.opus

# OS generated files
Thumbs.db
ehthumbs.db
.Spotlight-V100
.Trashes

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/
*.sublime-*

# YouTube player scripts (accidentally downloaded)
*-player-script.js
*-base.js

# Local planning docs (public repo - keep working docs out)
docs/superpowers/

# Voice stack: binaries/models/venv fetched by scripts/setup-voice.sh
tools/
*.onnx

# HITLIJN pool-resolve tooling lives under the otherwise-ignored tools/ dir;
# track the script itself, not the deck source data or its resumable state/output.
!hitlijn/tools/
hitlijn/tools/decks/
hitlijn/tools/resolve-state.json
hitlijn/tools/zonder-audio.txt
!hitlijn/tools/resolve-pools.mjs

# HITLIJN resolved pool data: productiedata, deck-afgeleid — blijft buiten de publieke repo; deploy kopieert.
hitlijn/data/

# Mixkit-geluiden: gebruik in de app mag, meeleveren met broncode niet (zie geluiden/HERKOMST.txt)
hitlijn/public/geluiden/mixkit/
hitlijn/public/geluiden/live-mixkit/
